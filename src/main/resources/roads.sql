-- MySQL Script generated by MySQL Workbench
-- Sat Oct 14 20:26:43 2017
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='TRADITIONAL,ALLOW_INVALID_DATES';

-- -----------------------------------------------------
-- Schema waydb
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Schema waydb
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `waydb` DEFAULT CHARACTER SET utf8 ;
USE `waydb` ;

-- -----------------------------------------------------
-- Table `waydb`.`TRANSPORT`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `waydb`.`TRANSPORT` (
  `ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `TYPE` VARCHAR(45) NOT NULL,
  `CREATED_AT` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`ID`),
  UNIQUE INDEX `ID_UNIQUE` (`ID` ASC))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `waydb`.`CROSSROADS`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `waydb`.`CROSSROADS` (
  `ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `NAME` VARCHAR(45) NOT NULL,
  `CREATED_AT` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`ID`),
  UNIQUE INDEX `ID_UNIQUE` (`ID` ASC))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `waydb`.`ROADS`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `waydb`.`ROADS` (
  `ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `NAME` VARCHAR(45) NOT NULL,
  `DAILY_CAPACITY` DOUBLE NOT NULL,
  `FROM_CROSSROAD_ID` INT UNSIGNED NOT NULL,
  `TO_CROSSROAD_ID` INT UNSIGNED NOT NULL,
  `HOTELS_COUNT` INT UNSIGNED NULL,
  `SEMAPHORES_COUNT` INT UNSIGNED NULL,
  `CREATED_AT` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`ID`),
  UNIQUE INDEX `ID_UNIQUE` (`ID` ASC),
  INDEX `fk_ROADS_CROSSROADS1_idx` (`FROM_CROSSROAD_ID` ASC),
  INDEX `fk_ROADS_CROSSROADS2_idx` (`TO_CROSSROAD_ID` ASC),
  CONSTRAINT `fk_ROADS_CROSSROADS1`
    FOREIGN KEY (`FROM_CROSSROAD_ID`)
    REFERENCES `waydb`.`CROSSROADS` (`ID`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `fk_ROADS_CROSSROADS2`
    FOREIGN KEY (`TO_CROSSROAD_ID`)
    REFERENCES `waydb`.`CROSSROADS` (`ID`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `waydb`.`AUTOBUSES`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `waydb`.`AUTOBUSES` (
  `ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `MODEL` VARCHAR(45) NOT NULL,
  `MAX_WEIGHT` DOUBLE NOT NULL,
  `CREATED_AT` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `TRANSPORT_ID` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`ID`),
  UNIQUE INDEX `ID_UNIQUE` (`ID` ASC),
  INDEX `fk_AUTOBUSES_TRANSPORT1_idx` (`TRANSPORT_ID` ASC),
  CONSTRAINT `fk_AUTOBUSES_TRANSPORT1`
    FOREIGN KEY (`TRANSPORT_ID`)
    REFERENCES `waydb`.`TRANSPORT` (`ID`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `waydb`.`BIKES`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `waydb`.`BIKES` (
  `ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `MODEL` VARCHAR(45) NOT NULL,
  `MAX_WEIGHT` DOUBLE NOT NULL,
  `CREATED_AT` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `TRANSPORT_ID` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`ID`),
  UNIQUE INDEX `ID_UNIQUE` (`ID` ASC),
  INDEX `fk_BIKES_TRANSPORT1_idx` (`TRANSPORT_ID` ASC),
  CONSTRAINT `fk_BIKES_TRANSPORT1`
    FOREIGN KEY (`TRANSPORT_ID`)
    REFERENCES `waydb`.`TRANSPORT` (`ID`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `waydb`.`CARS`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `waydb`.`CARS` (
  `ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `MODEL` VARCHAR(45) NOT NULL,
  `MAX_WEIGHT` DOUBLE NOT NULL,
  `CREATED_AT` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `TRANSPORT_ID` INT UNSIGNED NOT NULL,
  PRIMARY KEY (`ID`),
  UNIQUE INDEX `ID_UNIQUE` (`ID` ASC),
  INDEX `fk_CARS_TRANSPORT1_idx` (`TRANSPORT_ID` ASC),
  CONSTRAINT `fk_CARS_TRANSPORT1`
    FOREIGN KEY (`TRANSPORT_ID`)
    REFERENCES `waydb`.`TRANSPORT` (`ID`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
